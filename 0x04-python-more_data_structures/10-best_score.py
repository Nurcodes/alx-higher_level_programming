#!/usr/bin/python3
def best_score(a_dictionary):
    if a_dictionary is None:
        return(None)
    else:
        for i, j in sorted(a_dictionary.items()):
            high = i
        return(high)
